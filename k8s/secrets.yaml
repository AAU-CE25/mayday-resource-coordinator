apiVersion: v1
kind: Secret
metadata:
  name: mayday-secrets
  namespace: mayday
type: Opaque
stringData:
  # These will be overridden by GitHub Actions or kubectl
  POSTGRES_USER: "changeme"
  POSTGRES_PASSWORD: "changeme"
  SECRET_KEY: "changeme"
