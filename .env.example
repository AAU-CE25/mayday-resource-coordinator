# Example environment configuration for Mayday Resource Coordinator
# Copy this file to .env and adjust values as needed.

# --- Postgres settings (used by API service) ---
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# If your app uses a single DATABASE_URL, you can set it directly.
# This is optional; the API may build it from POSTGRES_* above when running in Docker.
# Example for local dev using Postgres:
# DATABASE_URL=postgresql+psycopg2://myuser:mypassword@localhost:5432/mydb
# Example for quick tests using SQLite (no Postgres needed):
# DATABASE_URL=sqlite:///./dev.db

# --- App security & environment ---
# Used for session/JWT signing in development. Change in production.
SECRET_KEY=your-super-secret-key-for-development
ENVIRONMENT=development

# Optional JWT configuration (uncomment if your app expects these)
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Admin bootstrap (used by control scripts/tests) ---
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe_123
ADMIN_NAME=Administrator
ADMIN_PHONE=0000000000

# --- Frontend config ---
# URL where the API is reachable by the Next.js frontend
#NEXT_PUBLIC_API_URL=http://localhost:8000

# --- External services (uncomment and adjust if needed) ---
# OSM_BASE_URL=https://nominatim.openstreetmap.org
# OSM_USER_AGENT=mayday-resource-coordinator-dev

# --- AWS/Infra (only if deploying) ---
# AWS_REGION=us-east-1
# ECR_REPOSITORY=mayday-resource-coordinator
# ECS_CLUSTER=mayday-cluster
# ECS_SERVICE=mayday-api
